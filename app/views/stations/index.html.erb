<% content_for(:title, "All Stations | Trimet") %>

<h1>Stations</h1>

<ul>
  <% @stations.each do |station| %>
    <li><%= link_to "#{station.name}", station_path(station) %>
    </li>
  <% end %>
</ul>


<%= link_to "New Station", new_station_path, :class=> "btn btn-success" %>
<%= link_to "View Lines", lines_path, :class=> "btn btn-default" %>
